import styled from "styled-components";

const MainBannerWrapper = styled.div`
`

const MainBanner = () => {
    return(
        <MainBannerWrapper>
            {'test!'}
        </MainBannerWrapper>
    );
}

export default MainBanner;